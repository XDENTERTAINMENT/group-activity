// let score = 72;
// let result = score >= 50 ? "pass" : "fail";
// console.log(result);

let cartTotal = 0;
let message = cartTotal > ? "Proceed to checkout": "Your cart is empty";
console. log( message); 


if(name)